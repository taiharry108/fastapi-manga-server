import { MangaSitePipe } from './manga-site.pipe';

describe('MangaSitePipe', () => {
  it('create an instance', () => {
    const pipe = new MangaSitePipe();
    expect(pipe).toBeTruthy();
  });
});
